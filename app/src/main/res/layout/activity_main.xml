<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">


    <TextView
        android:id="@+id/tv_select_target_img"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_marginTop="16dp"
        android:text="Please click here"
        android:textAlignment="center"
        android:textSize="20sp"
        app:layout_constraintTop_toTopOf="parent" />

    <ImageView
        android:id="@+id/iv_target_img"
        android:layout_width="match_parent"
        android:layout_height="250dp"
        android:minWidth="60dp"
        android:minHeight="60dp"
        android:scaleType="centerCrop"
        app:layout_constraintBottom_toTopOf="@+id/iv_result_img"
        app:layout_constraintTop_toBottomOf="@+id/tv_select_target_img" />


    <ImageView
        android:id="@+id/iv_result_img"
        android:layout_width="match_parent"
        android:layout_height="250dp"
        android:clickable="false"
        android:scaleType="centerCrop"
        app:layout_constraintBottom_toTopOf="@+id/btn_apply" />


    <Button
        android:id="@+id/btn_apply"
        android:layout_width="match_parent"
        android:layout_height="75dp"
        android:orientation="horizontal"
        android:text="Apply"
        app:layout_constraintBottom_toBottomOf="parent" />
</androidx.constraintlayout.widget.ConstraintLayout>